---
import { Button } from "@/components/ui/button";

interface Props {
	title?: string;
	description?: string;
}

const { title = "Lenin AGC | Portfolio", description = "Full Stack Web Developer Portfolio" } = Astro.props;

const pathname = Astro.url.pathname;
const isHome = pathname === "/";
const isBlog = pathname.startsWith("/blog");
---
<!doctype html>
<html lang="en" class="dark">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
		<meta name="description" content={description} />
		<link rel="preconnect" href="https://fonts.googleapis.com">
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
		<link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:ital,wght@0,100..800;1,100..800&display=swap" rel="stylesheet">
		<style>
			html,
			body {
				background: #02060a;
				color: #f2f7ff;
			}
		</style>
	</head>
	<body class="min-h-screen bg-background text-foreground font-mono antialiased flex flex-col selection:bg-livekit-blue/30 selection:text-livekit-cyan">
		<header class="sticky top-0 z-50 w-full border-b border-white/5 bg-livekit-dark/95 transition-colors duration-200">
			<div class="container flex h-20 items-center justify-between">
				<div class="flex items-center gap-2">
					<a href="/" class="group flex items-center gap-1.5 text-2xl font-bold tracking-tight transition-all duration-300">
						<span class="text-foreground group-hover:text-livekit-cyan transition-colors duration-300">Lenin</span>
						<span class="text-livekit-blue group-hover:text-livekit-cyan transition-colors duration-300">AGC</span>
					</a>
				</div>
				<nav class="hidden md:flex items-center gap-8 text-sm font-medium">
					<a href="/" class:list={["transition-colors hover:text-livekit-cyan", isHome ? "text-livekit-cyan" : "text-foreground/70 hover:text-foreground"]}>Home</a>
					<a href="/blog" class:list={["transition-colors hover:text-livekit-cyan", isBlog ? "text-livekit-cyan" : "text-foreground/70 hover:text-foreground"]}>Blog</a>
					<a href="https://github.com/IGUNUBLUE" target="_blank" rel="noreferrer" class="transition-colors hover:text-livekit-cyan text-foreground/70 hover:text-foreground">GitHub</a>
				</nav>
				<div class="flex items-center gap-4">
					<a 
						href="https://www.linkedin.com/in/lenin-agc" 
						target="_blank"
						class="hidden md:inline-flex items-center justify-center rounded-full bg-white/5 border border-white/10 px-6 py-2 text-sm font-medium text-foreground hover:bg-white/10 transition-all"
					>
						LinkedIn
					</a>
				</div>
			</div>
		</header>
		<main class="flex-1">
			<slot />
		</main>
		<footer class="border-t border-white/5 py-12 bg-livekit-dark/30">
			<div class="container flex flex-col items-center justify-between gap-6 md:h-24 md:flex-row">
				<div class="flex flex-col gap-2">
					<p class="text-sm font-medium tracking-tight">
						Lenin AGC <span class="text-muted-foreground font-normal">— Full Stack Web Dev</span>
					</p>
					<p class="text-xs text-muted-foreground">
						Built with Astro 5 & shadcn/ui. 2026 Edition.
					</p>
				</div>
				<div class="flex items-center gap-6 text-xs text-muted-foreground">
					<a href="#" class="hover:text-livekit-cyan transition-colors">Twitter</a>
					<a href="#" class="hover:text-livekit-cyan transition-colors">GitHub</a>
					<a href="#" class="hover:text-livekit-cyan transition-colors">LinkedIn</a>
				</div>
			</div>
		</footer>
	</body>
</html>

<style is:global>
	@import "../styles/global.css";
	
	:root {
		font-family: 'JetBrains Mono', monospace;
	}

	header {
		box-shadow: 0 4px 30px rgba(0, 0, 0, 0.1);
	}
</style>
